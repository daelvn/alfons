# alfons.look

Gets the path for a module, specifically tailored for Alfons
## API



| Element | Summary |
|---------|---------|
| **Functions** |  |
| [look](#look) | [`makeLook`](/alfons/look#makeLook) with `package.path` applied by default. |
| [makeLook](#makeLook) | Generate a function that looks for a module in a package path |

<div markdown class='fir-symbol fancy-scrollbar'>
### <strong>look</strong>&nbsp;
<span class='annotate'>:: module:string -> content:string|nil, err:string|nil</span>
</div>


[`makeLook`](/alfons/look#makeLook) with `package.path` applied by default.

- [`makeLook`](/alfons/look#makeLook) with `package.path` applied by default.

<div markdown class='fir-symbol fancy-scrollbar'>
### <strong>makeLook</strong>&nbsp;
<span class='annotate'>:: gpath:string -> module:string -> content:string|nil, err:string|nil</span>
</div>


Generate a function that looks for a module in a package path

- Generate a function that looks for a module in a package path
